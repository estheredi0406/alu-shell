#!/usr/bin/env bash
# Displays a simplified list of currently running processes for all users, focusing on essential details.

# Use ps -ef to list all processes, and awk to format the output.
ps -eo pid,user,%cpu,%mem,vsz,rss,tty,stat,start,time,args --sort=-start_time | awk '
BEGIN { FS = " "}
NR == 1 { print $0; next } # Skip the header
{
    # Format the output to show only essential details in a concise manner.
    printf("%-10s %-10s %-20s\n", $1, $2, $3);
}'
